
@{
    ViewBag.Title = "Index";
}

<div class="container">
    <div class="row mt-4">
        <div class="col-6">
            <button type="button" class="btn btn-outline-primary m-auto p-5">
                @Html.ActionLink("Anag.Trasgressori", "Trasgressori", "Home", htmlAttributes: new { @class = "text-decoration-none nav-link active" })
            </button>
        </div>
            <div class="col-6">
                <button type="button" class="btn btn-outline-secondary m-auto p-5">
                    @Html.ActionLink("Codici & Violazioni", "Violazioni", "Home", htmlAttributes: new { @class = "text-decoration-none nav-link" })
                </button>
            </div>
            <div class="row mt-4">
                <div class="col-6">
                    <button type="button" class="btn btn-outline-success m-auto p-5">
                        @Html.ActionLink("Ins.Verbale", "Verbali", "Home", htmlAttributes: new { @class = "text-decoration-none nav-link" })
                    </button>
                </div>
                <div class="col-6">
                    <button type="button" class="btn btn-outline-danger m-auto p-5">
                        @Html.ActionLink("Registro Verbali", "RegistroVerbali", "Home", htmlAttributes: new { @class = "text-decoration-none nav-link" })
                    </button>
                </div>
            </div>
        </div>

